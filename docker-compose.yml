version: '3.3'

services:
  app:
    container_name: pw
    image: tinyops/pw:1.0.1
    restart: always
    volumes:
      - ./pw.yml:/app/pw.yml
      #- ./locale.d:/app/locale.d
    ports:
      - "8080:8080"
